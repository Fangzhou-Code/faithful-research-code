# 修复后复查记录

日期：2026-09-20。验证范围是本 Skill、随附工具及一次隔离的模型生成科研小任务，不是论文复现或真实服务基准测试。

## 已落实的修复

- 进度状态机支持同轨道多个独立节点并发运行；必要依赖全部成功后才能启动汇合节点。
- 多进程通过本地认证记录端串行写入事件日志；无自动重发、无失败后切换直接写文件。
- 失败、阻塞、取消或观察丢失后停止新任务；在途任务仍可报告真实终态。停止状态保持，不因其他分支成功而解除。
- 条件未执行与成功分开；可接受跳过的依赖必须预声明。不满足的必要依赖会阻塞所有下游节点。
- 工作总量必须预声明，进度计数单调递增，不允许超额或提前成功。
- 网页显示并列分支、汇合箭头、多个活动节点和真实事件更新；窄窗口已目视检查。
- 审计器增加显式 JSON/TOML 配置检查及未验证范围说明；不执行配置，不把静态无告警当作完整证明。
- 明确准备提交与正式发布的边界：先完成可失败的观测写入，再执行最终原子发布。准备图完成不代表正式结果已经发布。

## 执行过的验证

| 检查 | 结果 | 证据边界 |
|---|---|---|
| `python -m unittest discover -s faithful-research-code/tests -q` | 39 项通过 | 包含真实本地子进程同时提交事件、依赖等待、失败传播、配置解析和只读查看器接口 |
| skill-creator `quick_validate.py faithful-research-code` | 通过 | 仅技能格式与元数据检查 |
| 审计工具检查 scripts 和 tests，`--min-severity low --fail-on medium --show-suppressed` | 退出码 0；无未处理的中/高等级项 | 18 个低等级项均为工具元数据/可选字段读取；3 个显式错误返回边界有理由注释，并保留在报告中 |
| 独立开发代理按 skill 生成的 SNIS 小任务 | 7 个测试方法通过 | 本地固定数据及模拟函数；不是真实 HTTP/LLM 服务测试 |
| 上述科研任务使用修订后的 tracker 重跑 | 7 个测试方法通过 | 验证公式、有效域、完整覆盖、失败停止、配对随机流和 Bootstrap 隔离 |
| 额外注入最终原子发布失败 | 通过 | 没有正式报告；暂存报告与 traceback 保留 |
| 浏览器检查 | 通过 | 观察两个运行节点、分支独立完成、窄窗口分叉/汇合布局及事件自动刷新 |

前向任务中，第二个样本模拟 429、500、超时或格式错误时，只发生前两个样本各一次调用，不请求第三个样本，不生成报告；保留已获得原始数据与报错。缺失的消融因素和 Bootstrap 区间方法被明确列为未实现，不自行补造。手工可核对的有效 SNIS 结果为 5。

该任务还暴露了“先发布结果，再在上下文退出时写进度”的集成问题；已在规范中补上明确提交顺序，并使用观测写入失败及最终发布失败两类注入检查。

本机独立前向验证产物位于：`C:/Users/94518/.codex/visualizations/2026/09/20/01a0bd67-1a13-7723-a211-ea26ad2da105/forward-eval/`。原始报告为 `FORWARD-REPORT.md`，原始命令/哈希为 `evidence-manifest.json`；修订后 tracker 的复查位于 `current-tracker-validation/`。这些是本机证据，不属于安装包的运行依赖。

## 最小方案补充与再次复查

本轮增加两个标准库脚本：可选的本地命令监督器，以及可重复使用的科研候选代码评测入口。静态分析盲区通过“具体运行检查＋未验证时阻止对应声明”处理，不声称静态分析已覆盖任意动态行为。

- 仓库测试现为 **47 项通过**，包含直接子进程并发、失败取消、超时、未声明检查阻断、最终凭据发布失败、过期心跳和候选评测正反例。
- 参考候选通过原有七项科研行为检查；故意把估计器改成常数零的候选被正确判为 FAIL，失败日志和评测报告保留。这是评测入口自检，不是一次新的独立模型生成。
- 11 个 Python 文件的语义审计无未处理的中/高等级项；20 个低等级候选属于工具元数据/可选字段读取，3 个错误报告边界保留了可审计注释。
- 当前 Windows 环境下 `taskkill` 无法确认取消；该实现已移除，改为直接操作运行器持有的子进程句柄并等待退出。实际失败取消测试通过，无自动回退到另一科研执行路径。
- 本地监督器显式限定 `direct_children_only`。它不冒充远程任务或任意嵌套进程树调度器；现有方法需要这些能力时保留原有合适调度器，不擅自改变方法。

## idea 文档与具体节点补充（2026-09-20）

- 新增未来科研项目使用的 `idea.md` 模板，以及主动澄清、需求编号、变更留痕和独立审阅规范。当前仓库是 skill 本身，没有虚构具体科研项目的需求文档。
- 统一处理 `UNKNOWN`：主动提问并阻塞依赖实现；参数化或排除必须有开发者明确决定。已明确且独立的工作可以继续。
- 节点可显示 `idea_refs` 和可展开的 `description`；科研节点具体到真实数据集/划分或算法子步骤。查看器不自动核验需求编号存在或代码语义一致，需实现者与独立审阅者核对。
- 仓库测试现为 **49 项通过**；新增检查覆盖节点需求信息在事件后保留，以及非法元数据在创建运行目录前被拒绝。skill 格式验证通过。
- 两个变更 Python 文件通过语义审计的中等级失败门槛；12 个低等级提示均为已有的可选观察元数据读取，1 个显式 HTTP 错误报告边界保留理由注释。
- 浏览器检查已验证两个并发模拟节点、展开后的完整说明、HTML 字符作为纯文本显示、事件刷新后保留展开状态和分支状态独立变化。使用隔离的 `idea-viewer-check`，模拟事件不代表科研实验已执行。
- 新建只读审阅智能体 `idea_review`，逐份检查本轮 11 个规范/模板/实现/测试文件，并独立运行进度工具的 15 项测试，全部通过；本轮可操作问题：**无**。没有 Git 基线，未独立执行浏览器检查、模型行为评测或科研实验。文件哈希与完整范围保留在本机 `idea-viewer-check/reviewed-files.json` 和 `independent-review.md` 中。

## 四项复查问题的修复与验证（2026-09-20）

1. **空测试误判 PASS**：评估器现在核对 `checks.json`，要求成功测试名称、数量、整体结果和进程退出码全部满足预期。候选导入时提前退出、跳过全部测试均判 FAIL。
2. **错误结果或缺失报告漏检**：新增成功发布检查，核对手算值 5、分子 30、分母 6、完整样本计数、返回值与正式报告一致、无待提交文件及进度完成。错误最终数值和取消发布两种变异均判 FAIL。
3. **鉴权阶段无限等待**：本地 IPC 保留标准库双向鉴权，用同一绝对期限覆盖连接、鉴权、发送和完整响应接收。默认总期限 30 秒；半包及持续零碎数据不能延长期限，不重试。记录端也对接受的连接设置通信期限。
4. **终止失败仍只显示 failed**：保留原始超时失败事件，另存 `termination_unknown`；查看器显示“状态未知／进程终止未确认；原始阶段状态：失败”。禁止后续节点和正式结果提交。

验证结果：

- 全部 **54 项仓库测试通过**，包含上述反例、鉴权停滞、半条响应、零碎数据和真实子进程终止失败注入。
- 新建独立审阅智能体 `fix_review`，独立运行相关三组 **28 项测试全部通过**；额外真实 socket 检查表明，鉴权已耗时后等待 ACK 仍共享同一期限，0.5 秒期限下约 0.516 秒超时。本轮可操作问题：**无**。
- 浏览器实际确认 HTML 同时显示原始失败及终止未知，下游保持阻塞。故障注入所建子进程已由测试清理逻辑终止，临时查看服务已停止；保留的图反映注入当时的观察状态。
- 7 个变更 Python 文件通过中等级语义审计门槛；15 个低等级候选为观察元数据读取，2 个错误报告/终止确认边界保留具体理由注释。
- 本机独立复查记录与文件哈希位于 `four-fixes-verification/independent-review.md`、`reviewed-files.json`。仍是本地工具及固定科研样例验证，不是论文复现或针对恶意候选的安全隔离证明。

## HTML 流程与 Token 监控补充（2026-09-20）

- 复用现有只读 HTML 查看器与单写入事件日志，增加请求所属阶段、模型、状态、输入/输出 Token，以及缓存/推理子项。服务端报告、本地估算、未知分别展示；未接入或缺失用量不记为零。
- 每个请求按最新累计快照统计，最终报告替换估算而不重复累计；失败时保留已知消耗。并发生产者使用原有认证记录端，无自动重发。请求未完成或失败时，所属阶段不能成功；停止后禁止登记新请求，在途请求仍能记录结局。
- 全部 **62 项仓库测试通过**，新增 8 项覆盖统计、未知值、非法事件、失败停止与真实本地记录端。skill 格式验证通过；3 个变更 Python 文件通过中等级语义审计门槛，13 个低等级提示为可选元数据读取，1 个 HTTP 错误报告边界保留具体理由注释。
- 独立审阅智能体 `dashboard_review` 发现阶段退出时成功事件被拒绝后未停止轨道的问题，已修复并覆盖直接写入与记录端两条路径。复查确认只对日志追加前的明确拒绝记录失败；通信超时或写入结局不明时不重试、不伪造失败事件。本轮复查未发现其他可操作问题。
- 浏览器已验证两个并发节点及自动刷新：模拟请求的估算 900/40 被最终报告 950/55 替换，报告合计更新为 2150/235；主分支完成后，消融仍运行，汇合节点仍等待。演示明确标注模拟数据，未请求外部模型。
- 接入方式见 `references/usage-dashboard.md`。能力限于已接入科研程序的请求，不自动拦截任意 SDK，不统计 Codex 对话用量，不自动计费或校验模型上下文窗口。可选命令监督器尚未接入子进程用量转发，不能声称自动覆盖其子进程请求。

## 四项定向修复：源码评估、增量监控、作用域与分位数（2026-09-20）

- **评估源码一致性**：运行前保存候选与测试源码快照，候选用同一份校验过的字节直接 `compile + exec`，报告哈希对应执行快照。旧 `.pyc`、同大小/秒级修改时间及运行期间原源码变化均有回归覆盖；原文件路径与本地辅助模块导入仍可用，辅助模块和依赖不因此自动冻结。
- **增量监控**：请求使用 ID 字典，累计用量通过旧、新快照差额维护；持久记录进程和查看器只消费新追加的事件，完整日志保留，独立 `snapshot/show` 仍可完整重建。页面每秒轻量检查 `/status`，阶段转换、显式批次/轮次进度、请求失败及监督器观察变化才拉取完整状态并重绘。
- **失败边界**：拒绝事件不改变缓存；不确定持久化保留 `.writer.lock`，缓存驱逐或新进程仍不能继续写入。部分尾行暂时不可读；截断、替换拒绝，不自动修复日志或重启实验。
- **作用域与统计规则**：局部导入/参数/变量绑定不再污染外层别名；函数内数值导入不被当作模块已执行的导入；注解赋值按实际顺序先检查右侧。四种 NumPy 分位数 API 的 `method` 正确定位第六个实参，对动态展开参数要求核查。
- **测试与审阅**：全部 **81 项测试通过**。独立审阅发现并推动修复了不确定写入状态随缓存驱逐丢失的问题；复查实际验证另一运行驱逐缓存、新 Python 进程续写均被拒绝。独立评估器复查 5 项测试通过；100 个请求多次用量更新的增量/全量差分一致。skill 格式校验通过；7 个变更 Python 文件通过中等级语义审计门槛，19 个低等级候选为分析器/观察器元数据处理，4 个具体错误报告或停止边界保留理由注释。
- **本机性能测量**：10,000 个模拟请求、每请求登记与完成两条事件，首次重建约 **0.362 秒**，无新增事件的轻量状态检查平均约 **0.00020 秒**，缓存已就绪时单次新增事件写入（含 fsync）约 **0.025 秒**，查看器读取新增事件约 **0.0067 秒**。这些是本机一次基准结果，不是其他机器或实际 LLM 服务的延迟保证。
- **浏览器验证**：模拟请求写入后事件数增加、面板保持不变；显式批次进度事件后，报告 Token 从 2150/235 更新为 2200/245，主分支仍完成、消融分支仍运行。未调用外部模型，演示明确标注模拟数据。
- 本轮按请求只修改上述四项及其文档/测试，根目录压缩包尚未重新生成，仍不能作为当前源码版本的安装包。

## 当前仍需区分的能力边界与待定项

1. 静态分析仍不能证明跨模块动态配置、自定义 SDK、未导出的 YAML/启动器与真实请求次数；当前新增配置检查没有消除这些盲区。
2. 已提供跨候选可复用的评测入口，但尚未实测新的模型矩阵、自动触发准确率或真实外部服务行为；不能把工具存在当作这些结果已经通过。
3. 可选运行器已覆盖本地直接子进程取消、观察与提交检查；任意嵌套进程、远程任务和被硬杀的监督器仍超出这一最小运行器的保证范围。
4. 仓库仍未指定许可证，使用、修改和再分发权限尚未明确。
